#include <stdio.h>
#include <stdlib.h>

int add_substract(struct mat_structure, struct mat_structure, int, struct mat_structure*);
int multiplier(struct mat_structure, struct mat_structure, struct mat_structure*);